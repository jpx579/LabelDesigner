<Window x:Class="LabelDesigner.Views.AddLabelWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LabelDesigner.Views"
        Title="添加标签" 
        Height="150" 
        Width="300" 
        WindowStartupLocation="CenterOwner" 
        ResizeMode="NoResize">
    <StackPanel Margin="10">
        <TextBlock Text="请输入标签名称：" Margin="0,0,0,5"/>
        <TextBox x:Name="NameTextBox" Margin="0,0,0,10"/>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="确定" Width="70" Margin="0,0,5,0" Click="OnConfirmClick"/>
            <Button Content="取消" Width="70" Click="OnCancelClick"/>
        </StackPanel>
    </StackPanel>
</Window>
